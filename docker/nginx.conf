# This is our OWN main nginx.conf, we control everything.

user nginx;

worker_processes auto;

pid /var/run/nginx.pid;

# Redirect Nginx logs to Docker's log collector
# error_log 允许在 main 上下文，access_log 需放在 http/server/location 内
error_log  /dev/stderr warn;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /dev/stdout main;

    sendfile        on;
    tcp_nopush      on;
    keepalive_timeout 65;

    client_max_body_size 32m;

    # Map Upgrade header to a safe Connection value for ws support
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    # Our upstream definitions, correctly placed in the http context
    upstream next_app {
        server 127.0.0.1:3000;
    }

    upstream backend_app {
        server 127.0.0.1:4000;
    }

    # Our server block, correctly placed in the http context
    server {
        listen 0.0.0.0:7860;
        server_name _;

        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;

        location /services/ {
            proxy_pass http://backend_app/;
            proxy_buffering off;
            proxy_cache off;
            proxy_set_header Connection '';
            proxy_read_timeout 600s;
            proxy_send_timeout 600s;
        }

        location = /services {
            return 307 /services/;
        }

        location /_next/static/ {
            proxy_pass http://next_app/_next/static/;
            add_header Cache-Control "public, max-age=31536000, immutable";
        }

        location /_next/image {
            proxy_pass http://next_app/_next/image;
        }

        location /api/ {
            proxy_pass http://next_app/api/;
        }

        location / {
            proxy_pass http://next_app;
        }
    }
}
