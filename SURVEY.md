# AI+互动游戏挑战赛
## 赛题介绍
### AI游戏创作工具赛道
主题： “降低门槛，普惠创作”

开发面向大众的AI游戏创作工具或平台，让非程序员也能轻松创作游戏。工具需集成AI能力，如自动生成代码、素材、剧情等功能，通过可视化操作界面和智能辅助功能，真正实现游戏创作的普惠化。参赛者须将工具部署在魔搭平台创空间（创空间名字需包含“AI+互动游戏挑战赛-赛道二”），并提交作品介绍研习社文章，包括使用教程及示例作品视频等，展示如何通过技术创新让游戏创作变得简单易用，推动全民游戏创作时代的到来。

赛题二：AI游戏创作工具赛道展示重点
工具产品演示：现场操作AI游戏创作工具/平台，展示可视化界面
创作流程展示：演示从零开始创作游戏的完整过程，突出AI辅助功能
示例作品展示：运行用该工具创作的游戏示例，证明工具的实用性
导出功能演示：展示工具如何导出可运行的游戏，体现工具链的完整性

评分维度 分值 评分标准
技术创新性 40分 • 项目能够充分利用大模型的生成能力，实现技术上的创新应用
• 融合利用多种技术能力，在AI游戏上挖掘更深层次的技术可能性
工具链完成度 20分 AI创作工具链是否可以完整生成游戏端到端代码和项目，或者AI在游戏内容中的参与占比
AI逻辑跟随能力 20分 • AI逻辑严谨程度
• AI指令（逻辑生成、多Agent交互、文生图等的）的跟随能力
工具链复用 20分 • 工程代码是否结构清晰
• 模块、mcp、prompt等原子能力是否结构清晰便于开源复用
总分 100分

## 特别加分项

加分项目 分值 评分标准
开源贡献 +5分 开源代码或工具
社会价值 +5分 教育、公益、无障碍设计
商业潜力 +3分 具备明确变现路径
国际化 +2分 支持多语言或面向全球市场

---

我对此展开的调研：

# Question 1:
- 我正在参加一个 Hackathon 比赛，想要制作一个视觉小说VNG制作向导，需要设计和实现哪些部分？（我隐约感觉视觉小说或者说视觉小说引擎 需要有一些基本概念先让我意识到，比如剧本、舞台、角色之类的？请帮我梳理一下，这样我才能知道开发设计时要考虑的方向和角度）
- 进一步地，要融入 LLM、Agent、MCP 等 AI 技术增加创新性和趣味性
# Answer 1:

## 🎮 一、视觉小说引擎的核心概念模型

视觉小说本质上是一个**分支叙事系统**，以下是需要理解的核心领域概念：

### 1. 📜 剧本层 (Script/Story Layer)

| 概念 | 说明 |
|------|------|
| **故事线 (Plot Line)** | 主线/支线剧情的抽象，可以有多条并行 |
| **章节 (Chapter)** | 故事的大段落划分 |
| **场景 (Scene)** | 故事发生的"时空单位"，一个场景内背景/角色相对固定 |
| **对话节点 (Dialogue Node)** | 最小叙事单元：谁说了什么、表情如何、配什么音效 |
| **分支 (Branch/Choice)** | 玩家选择点，导向不同的故事走向 |
| **条件 (Condition)** | 触发分支的逻辑判断（好感度、物品、flag等）|
| **变量 (Variable/Flag)** | 全局状态存储：角色好感度、道具、剧情flag |

### 2. 🎭 舞台层 (Stage/Presentation Layer)

| 概念 | 说明 |
|------|------|
| **背景 (Background)** | 场景的环境图（教室、咖啡厅、夜空等）|
| **立绘 (Character Sprite)** | 角色的2D形象，通常有多个表情/姿态变体 |
| **表情差分 (Expression)** | 同一立绘的不同表情版本 |
| **特效 (Effect)** | 画面震动、闪白、雨雪、转场动画等 |
| **CG (Event CG)** | 关键剧情的全屏插画 |
| **UI层** | 对话框、选项按钮、存档界面、设置等 |

### 3. 🎵 音频层 (Audio Layer)

| 概念 | 说明 |
|------|------|
| **BGM** | 背景音乐，按场景/情绪切换 |
| **SE (Sound Effect)** | 音效（门声、脚步、心跳等）|
| **Voice** | 角色配音（可选）|

### 4. 👤 角色层 (Character Layer)

| 概念 | 说明 |
|------|------|
| **角色档案 (Character Profile)** | 姓名、立绘集合、性格设定、初始属性 |
| **角色状态** | 当前表情、位置、好感度等运行时数据 |

### 5. 🔄 游戏系统层 (Game System)

| 概念 | 说明 |
|------|------|
| **存档/读档 (Save/Load)** | 游戏状态持久化 |
| **回想/CG鉴赏 (Gallery)** | 已解锁内容的回顾 |
| **成就系统** | 多结局收集、隐藏要素 |
| **历史记录 (Backlog)** | 对话回看功能 |

---

## 🏗️ 二、VNG制作平台需要设计的模块

基于以上概念，你的**平台**需要设计以下子系统：

```
┌─────────────────────────────────────────────────────────────┐
│                    VNG Studio 架构                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  剧本编辑器  │  │  角色管理器  │  │  素材库管理  │         │
│  │ (节点式/    │  │ (立绘/表情   │  │ (背景/音频   │         │
│  │  可视化流程) │  │  /人设卡)    │  │  /CG/特效)   │         │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘         │
│         │                │                │                 │
│         ▼                ▼                ▼                 │
│  ┌─────────────────────────────────────────────────┐       │
│  │              项目状态管理中心                     │       │
│  │        (故事数据结构 + 资源引用)                  │       │
│  └─────────────────────────────────────────────────┘       │
│         │                │                │                 │
│         ▼                ▼                ▼                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │  实时预览器  │  │  导出引擎   │  │  AI 能力层   │         │
│  │ (所见即所得) │  │ (生成可运行 │  │ (剧情/立绘   │         │
│  │              │  │  游戏包)    │  │  /配音生成)  │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 核心编辑器设计方向

| 模块 | 推荐实现形式 | 前端技术亮点 |
|------|-------------|-------------|
| **剧本编辑器** | 节点式流程图（类Figma/蓝图） | React Flow / Vue Flow + 拖拽 |
| **角色管理** | 卡片式管理 + 立绘预览 | 图层管理、表情切换预览 |
| **场景编辑** | 舞台预览 + 时间轴 | Canvas渲染、Tween动画 |
| **对话编辑** | 富文本 + 特效标记语法 | Monaco Editor / 自定义DSL |
| **实时预览** | 嵌入式播放器 | 状态机驱动的渲染引擎 |

---

## 🤖 三、AI技术融合方案（核心创新点）

这是你们区别于传统工具的**杀手锏**！以下是分层的AI能力设计：

### 🧠 Level 1: AI 辅助生成（基础能力）

| 功能 | 技术实现 | 亮点 |
|------|---------|------|
| **AI剧情生成** | LLM生成对话/分支剧情 | 输入大纲→生成完整剧本 |
| **AI立绘生成** | 文生图（SD/MJ API） | 根据角色设定生成立绘 |
| **AI表情差分** | 图像编辑模型 | 一张立绘→多表情变体 |
| **AI背景生成** | 文生图 | 场景描述→背景图 |
| **AI配音** | TTS（Edge TTS/Fish Audio） | 角色台词→语音 |
| **AI BGM** | 音乐生成（Suno API） | 情绪标签→背景音乐 |

### 🎯 Level 2: Agent 工作流（技术深度）

这是**最能体现技术创新**的部分！设计一个多Agent协作系统：

```
┌─────────────────────────────────────────────────────────────┐
│                   Multi-Agent 创作系统                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   用户输入: "创作一个校园悬疑故事，主角是转学生侦探"           │
│                         │                                   │
│                         ▼                                   │
│   ┌─────────────────────────────────────────────┐           │
│   │            🎬 Director Agent (总导演)        │           │
│   │    负责整体规划、协调各Agent、质量把控         │           │
│   └─────────────────────────────────────────────┘           │
│          │              │              │                    │
│          ▼              ▼              ▼                    │
│   ┌───────────┐  ┌───────────┐  ┌───────────┐              │
│   │📖 Writer  │  │🎨 Artist  │  │🎵 Composer│              │
│   │  Agent    │  │  Agent    │  │  Agent    │              │
│   │           │  │           │  │           │              │
│   │ 负责剧情  │  │ 负责视觉  │  │ 负责音频  │              │
│   │ 对话/分支 │  │ 立绘/背景 │  │ BGM/音效  │              │
│   └─────┬─────┘  └─────┬─────┘  └─────┬─────┘              │
│         │              │              │                    │
│         ▼              ▼              ▼                    │
│   ┌─────────────────────────────────────────────┐           │
│   │              📦 Assembler Agent              │           │
│   │     整合所有输出 → 生成完整VNG项目结构         │           │
│   └─────────────────────────────────────────────┘           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 🔧 Level 3: MCP Tools 设计（工程亮点）

MCP (Model Context Protocol) 可以让Agent获得"工具使用"能力。设计以下MCP工具集：

```typescript
// MCP Tools 示例定义
const VNG_MCP_TOOLS = {
  // 项目管理工具
  "vng_create_project": { /* 创建新VNG项目 */ },
  "vng_get_project_structure": { /* 获取项目结构 */ },
  
  // 剧本操作工具
  "vng_add_scene": { /* 添加场景 */ },
  "vng_add_dialogue": { /* 添加对话节点 */ },
  "vng_add_choice": { /* 添加分支选项 */ },
  "vng_set_variable": { /* 设置剧情变量 */ },
  
  // 角色工具
  "vng_create_character": { /* 创建角色 */ },
  "vng_add_expression": { /* 添加表情差分 */ },
  
  // 资源工具
  "vng_generate_background": { /* AI生成背景 */ },
  "vng_generate_sprite": { /* AI生成立绘 */ },
  "vng_generate_voice": { /* AI生成配音 */ },
  
  // 预览与导出
  "vng_preview_scene": { /* 预览指定场景 */ },
  "vng_export_game": { /* 导出游戏包 */ },
}
```

**这样设计的好处：**
1. Agent可以通过调用工具**直接操作项目**
2. 用户可以用自然语言指挥AI创作
3. MCP协议是标准化的，代码可复用性强

### 💬 Level 4: 交互式AI创作（用户体验）
略
---

## 🎯 四、推荐的技术栈与架构

```
前端 (核心展示层)
├── React/Vue 3 + TypeScript
├── React Flow / Vue Flow (节点式剧本编辑)
├── Zustand/Pinia (状态管理)
├── Monaco Editor (脚本编辑)
├── PixiJS / Konva (舞台渲染预览)
└── Framer Motion (动画效果)

AI层 (核心能力层)
├── LangChain / LangGraph (Agent编排)
├── 通义千问/GLM/Qwen (LLM)
├── Stable Diffusion API (图像生成)
├── Edge TTS / Fish Audio (语音合成)
└── MCP Server (工具协议)

后端 (轻量支撑)
├── Python FastAPI / Node.js
├── SQLite / 浏览器IndexedDB (项目存储)
└── 文件导出服务
```

---

## 🏆 五、竞赛得分策略

根据评分维度，你们的亮点应该是：

| 评分维度 | 对应策略 |
|---------|---------|
| **技术创新性 (40分)** | Multi-Agent协作系统 + MCP工具链 |
| **工具链完成度 (20分)** | 从创意→编辑→预览→导出的完整流程 |
| **AI逻辑跟随 (20分)** | Chat驱动创作 + Agent精确调用工具 |
| **工具链复用 (20分)** | MCP Tools 模块化、Prompt模板开源 |
| **加分项** | GitHub开源(+5)、教育价值(+5)、多语言(+2) |

# Question 2:
这个设计太完整了，我们要适当地取舍。比如不需要特效、不需要配音、BGM、不一定要允许用户自然语言调用工具等
我需要一个3天以内 vibe coding 即可完成的版本

# Answer 2:
这是一个非常务实的决策！在 Hackathon 中，**"完成度 > 完美度"**。

既然要 **Vibe Coding** (靠 Prompt 和 Spec 快速生成) 且只有 **3天**，我们需要把核心聚焦在 **“从创意到可玩游戏的端到端自动化”** 上。

舍弃细枝末节，保留最核心的骨架。我们将打造一个 **"AI 视觉小说生成器 (AI Visual Novel Generator)"**。

---

### 🚀 极简版 VNG 架构 (3-Day Hackathon Edition)

**核心理念**：用户输入一句话 -> AI 生成结构化 JSON 剧本 & 自动绘图 -> 自动组装成游戏 -> 用户微调 -> 运行。

#### 1. 核心数据结构 (The "Soul")

为了方便 LLM 生成和前端渲染，我们把整个游戏抽象为一个单一的 JSON 对象。这是你 Prompt Engineering 的核心目标。

```typescript
// 极简游戏定义 Schema
interface GameProject {
  title: string;
  introduction: string;
  // 角色表：AI先生成这个，以此保持角色一致性
  characters: {
    id: string;
    name: string;
    description: string; // 用于生成立绘的 prompt
    avatarUrl?: string;  // 对应的立绘图片
  }[];
  // 场景表：背景图
  backgrounds: {
    id: string;
    description: string; // 用于生成背景的 prompt
    imageUrl?: string;
  }[];
  // 剧本流：核心剧情
  script: ScriptNode[];
}

interface ScriptNode {
  id: string;
  characterId: string; // 谁在说话 (空代表旁白)
  text: string;        // 台词
  backgroundId: string; // 当前背景
  // 极简分支逻辑：简单的跳转
  choices?: {
    text: string;
    nextScriptId: string;
  }[];
  nextScriptId?: string; // 线性剧情的下一句
}
```

#### 2. 只有三个核心页面 (The "Skeleton")

不需要复杂的路由，只需要三个状态：

1.  **Idea Input (创意输入)**
    *   一个漂亮的输入框：“我想做一个关于...的故事”。
    *   配置项：画风选择（二次元/水墨/像素）、长度（短篇/长篇）。
2.  **Generation Dashboard (生成看板 - 技术含量展示区)**
    *   **这是给评委看的High点**。不要让用户干等，要把 AI 在后台做的事情可视化出来。
    *   左边显示 Log：“正在设计角色... 正在构思大纲... 正在绘制场景...”
    *   右边实时弹出生成的 JSON 片段和图片预览。
3.  **Editor & Player (编辑与试玩)**
    *   **左侧/底部**：简单的流式编辑器（像聊天记录一样修改台词）。
    *   **右侧/中间**：实时游戏画面。
    *   **功能**：点击图片可以“重新生成”，点击文本可以修改。

#### 3. AI 工作流 (The "Agent" Magic)

用简单的 **Chain of Thought (思维链)** 代替复杂的 Agent 框架。前端直接串行调用 LLM 即可：

*   **Step 1: 设定生成 (Architect)**
    *   Prompt: "你是一个游戏策划，基于用户输入 '{input}'，设计 3 个主要角色和 4 个关键场景，输出 JSON。"
*   **Step 2: 剧本生成 (Writer)**
    *   Prompt: "你是一个剧本家，基于上一步的角色和场景，创作一个有 2 个分支结局的短篇剧本，输出 JSON 节点列表。"
*   **Step 3: 资产具体化 (Artist - 并行处理)**
    *   遍历生成的 JSON，提取 `description` 字段。
    *   **并发调用** 生图 API (SD/Midjourney/Flux) 将文字转为图片 URL。
    *   回填到 JSON 中。
*   **Step 4: 组装 (Assembler)**
    *   前端将生成好的 JSON 灌入 React 状态，游戏直接可玩。

#### 4. Vibe Coding 实施指南 (如何利用 AI 快速写代码)

你可以直接把下面的 Prompt 丢给 Cursor/Windsurf 来写代码：

**Day 1: 基础架构与编辑器**
> **Prompt 示例**:
> "创建一个 Next.js 项目，使用 Tailwind 和 Zustand。
> 请定义一个 `types.ts` 包含 Visual Novel 的数据结构（Game, Character, Scene, Node）。
> 然后写一个 `GameEngine` 组件，接收 Game 数据，根据当前 Node 渲染背景图、立绘和对话框。
> 需要支持点击屏幕继续，点击选项跳转 Node。"

**Day 2: LLM 接入与结构化输出**
> **Prompt 示例**:
> "写一个 API Route `/api/generate-script`。
> 使用 Vercel AI SDK 或 OpenAI SDK。
> System Prompt 是：'你是一个视觉小说引擎的后端，请严格按照以下 JSON Schema 输出剧情...'。
> 用户输入主题后，流式返回生成的 JSON。
> 并在前端展示生成的进度。"

**Day 3: 对接生图与优化**
> **Prompt 示例**:
> "在生成脚本后，解析所有的 Character 和 Background。
> 为每个资源创建一个异步任务，调用生图 API (Mock 一个 fetch 函数即可，或者接真实的)。
> 在 UI 上显示这些图片的加载状态。
> 当图片 URL 返回后，自动更新 Zustand store，让画面亮起来。"

---

### 💡 为什么这个方案能赢？

1.  **符合 "降低门槛" (赛题主题)**：用户只输入一句话，游戏就出来了，这就是极致的降低门槛。
2.  **视觉冲击力强**：从纯文本变成有图有剧情的游戏，演示效果非常好。
3.  **体现 LLM 能力**：展示了 LLM 处理复杂结构化数据（JSON）和逻辑一致性（剧情连贯）的能力。
4.  **前端技术栈**：React 组件化渲染游戏引擎，属于前端工程师的舒适区，代码质量可控。

# Question 3:
等等！是不是 Github 上已经存在类似的开源项目/解决方案了？

# Answer 3:
好问题！在动手之前先做技术调研是非常明智的。让我帮你搜一下现有的开源方案。

[3 tools called]

让我再深入搜一下 AI 结合的具体项目：

[7 tools called]

---

## 📊 开源项目调研结果

好消息！经过搜索，我发现现有项目**分为两个阵营**，但**中间存在明显的空白地带**——这正是你们的机会！

### 🔵 阵营一：传统视觉小说引擎 (手动创作工具)

| 项目 | 技术栈 | 特点 | 局限性 |
|------|--------|------|--------|
| **[Ren'Py](https://github.com/renpy/renpy)** | Python | 最流行、生态完善、跨平台 | 需要写脚本、准备素材 |
| **[WebGAL](https://github.com/OpenWebGAL/WebGAL)** ⭐ | TypeScript/Web | 无需编程、可视化编辑、国产 | 仍需手动准备剧本和素材 |
| **[NVL Maker](https://www.nvlmaker.net/)** | KiriKiri/KAG | 图形化界面 | 较老、Windows only |
| **[Twine](https://github.com/klembot/twinejs)** | HTML/JS | 交互式故事编辑 | 偏文字、无立绘系统 |
| **[VNVE](https://github.com/vnve/vnve)** | Web | 视觉小说→视频生成 | 不是游戏，是视频输出 |

**这类工具的共同点**：用户需要**自己写剧本 + 自己准备素材**，工具只负责"组装和渲染"。

---

### 🟣 阵营二：AI 驱动的交互式叙事 (实时生成)

| 项目 | 技术栈 | 特点 | 局限性 |
|------|--------|------|--------|
| **[SillyTavern](https://github.com/SillyTavern/SillyTavern)** | Node.js | 角色扮演聊天前端 | 是聊天工具，不是游戏生成器 |
| **[KoboldAI](https://github.com/KoboldAI/KoboldAI-Client)** | Python | 本地LLM文字冒险 | 纯文字、无视觉元素 |
| **AI Dungeon** (闭源) | - | 实时AI剧情生成 | 商业产品、无图像 |
| **NovelAI** (闭源) | - | 故事写作+AI绘图 | 商业产品、分离式工具 |

**这类工具的共同点**：专注**实时对话/交互**，不是"生成完整可导出的游戏"。

---

### 🎯 市场空白 = 你们的机会！

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   传统引擎                          AI交互叙事                   │
│   (手动创作)                        (实时生成)                   │
│                                                                 │
│   ┌─────────┐                       ┌─────────┐                 │
│   │ Ren'Py  │                       │SillyTav │                 │
│   │ WebGAL  │      🎯 空白地带       │KoboldAI │                 │
│   │  Twine  │  ←──────────────────→ │AI Dungeon│                 │
│   └─────────┘                       └─────────┘                 │
│                                                                 │
│         │              ↑                    │                   │
│         │              │                    │                   │
│         │      ┌───────────────┐            │                   │
│         │      │  你们的产品   │            │                   │
│         │      │               │            │                   │
│         │      │ AI 一键生成   │            │                   │
│         │      │     ↓         │            │                   │
│         └─────→│ 可视化编辑    │←───────────┘                   │
│                │     ↓         │                                │
│                │ 导出可玩游戏  │                                │
│                └───────────────┘                                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**你们的差异化定位**：

| 现有方案 | 你们的创新 |
|---------|-----------|
| 用户写剧本 → 引擎渲染 | **AI写剧本** → 用户微调 → 引擎渲染 |
| 用户准备素材 | **AI生成素材** (立绘/背景) |
| 分离的工具链 | **端到端一体化**：输入→生成→编辑→导出 |
| 要么纯文字，要么纯手动 | **结构化JSON + 自动配图** |

---

### 🚀 具体策略建议

#### 方案A：从零做（纯原创，技术分高）

**优点**：完全原创、架构清晰、评委友好
**缺点**：工作量大

核心卖点：
- 输入一句话 → AI生成完整JSON剧本 → AI配图 → 导出可玩HTML游戏
- 可视化节点编辑器微调

---

#### 方案B：基于 WebGAL 二次开发（借力打力，完成度高）

**WebGAL 是国产开源项目，TypeScript写的，和你们技术栈吻合！**

**优点**：渲染引擎和导出功能已完成、可以专注AI能力
**缺点**：创新性评分可能稍低

核心策略：
1. Fork WebGAL
2. 加一个 **AI生成层**：输入主题 → 生成 WebGAL 格式的脚本文件
3. 加一个 **可视化编辑器**（WebGAL自带的编辑器偏传统）
4. 对接文生图API自动生成立绘

---

#### 方案C：极简原创 + 轻量运行时（推荐 ⭐）

**我更推荐这个**，因为 Hackathon 时间有限，且你们是前端出身：

1. **自己写一个极简的 JSON-driven 视觉小说播放器**（React组件，半天搞定）
2. **重点做AI生成流程的可视化**（评委看得见的亮点）
3. **导出为单HTML文件**（包含JSON数据和播放器代码）

这样：
- 代码100%原创 → 技术创新分高
- 架构清晰可复用 → 工程分高
- AI是核心而非附加功能 → 切题

---

### 📋 总结

| 问题 | 答案 |
|------|------|
| 有没有现成的AI视觉小说生成器？ | **几乎没有完整的开源方案** |
| 有没有传统VN引擎？ | 有很多（Ren'Py, WebGAL等） |
| 你们的创新空间？ | **AI端到端生成 + 可视化编辑 + 一键导出** |
| 是否需要重复造轮子？ | 播放器可以极简自己写，或基于WebGAL |

---

好，就用方案C
TS语言，monorepo架构，把创作向导主界面、剧本编辑器、游戏播放器、剧本创作Agent等模块拆分到不同的子包中（按需可能还要涉及微前端）
前端基于React、React-flow
Agent引擎可以使用Mastra (https://github.com/mastra-ai/mastra)

有一个别人给我的初步设计在 ARCHITECTURE.md 中，你可以先不看它！
因为它似乎缺失设定确认编辑阶段，然后 Agent 的设计也不太完善